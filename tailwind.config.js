const colors = require('tailwindcss/colors')

module.exports = {
  theme: {
    colors: {
      gray: colors.coolGray,
      white: colors.white,
      primary: '#FE9738',
      'primary-light': '#FFE5CD'
    }
  }
}
